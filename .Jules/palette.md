## 2024-05-24 - Calculator Accessibility
**Learning:** Custom calculator inputs should be `readonly` with an `aria-label` to prevent unexpected mobile keyboard popups and improve screen reader experience.
**Action:** Always add `readonly` and `aria-label` to custom calculator inputs to ensure a seamless and accessible user experience.
